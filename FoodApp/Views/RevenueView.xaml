<Page
    x:Class="FoodApp.Views.RevenueView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="using:FoodApp"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:viewModels="using:FoodApp.ViewModel"
    mc:Ignorable="d"
    Background="#2E2E2E"> <!-- Dark background -->

    <Page.DataContext>
        <viewModels:RevenueViewModel />
    </Page.DataContext>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- Header -->
        <Grid Grid.Row="0" Margin="20" Background="#3C3C3C" Padding="10" CornerRadius="5">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <Button
                HorizontalAlignment="Left"
                VerticalAlignment="Top"
                Click="BackButton_Click">
                <Button.Content>
                    <SymbolIcon Symbol="Back" />
                </Button.Content>
            </Button>
            <TextBlock
                Grid.Column="1"
                Margin="10,0,0,0"
                VerticalAlignment="Center"
                FontSize="30"
                FontWeight="Bold"
                Text="Revenue Analysis" 
                Foreground="White"/> <!-- Lighter text color -->
        </Grid>

        <!-- Content -->
        <Grid Grid.Row="1" Margin="20">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <!-- Filters -->
            <StackPanel
                Grid.Row="0"
                Margin="0,0,0,20"
                HorizontalAlignment="Left"
                Orientation="Horizontal">
                <TextBlock
                    Margin="0,0,10,0"
                    VerticalAlignment="Center"
                    Text="From:" 
                    Foreground="White"/> <!-- Lighter text color -->
                <DatePicker
                    x:Name="FromDatePicker"
                    Width="200"
                    Margin="0,0,20,0" />
                <TextBlock
                    Margin="0,0,10,0"
                    VerticalAlignment="Center"
                    Text="To:" 
                    Foreground="White"/>
                <DatePicker x:Name="ToDatePicker" Width="200" />
                <Button
                    Margin="20,0,0,0"
                    Click="FilterButton_Click"
                    Content="Filter" 
                    Background="#4CAF50" 
                    Foreground="White" 
                    Padding="5,10" 
                    CornerRadius="3"/> <!-- Styled button -->
            </StackPanel>

            <!-- Table Header -->
            <Grid Grid.Row="1" Margin="0,0,0,30">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="2*" />
                    <ColumnDefinition Width="1*" />
                    <ColumnDefinition Width="1*" />
                </Grid.ColumnDefinitions>
                <TextBlock
                    Grid.Column="0"
                    FontSize="18"
                    FontWeight="Bold"
                    Text="Product Name" 
                    Foreground="White"/>
                <TextBlock
                    Grid.Column="1"
                    HorizontalAlignment="Right"
                    FontSize="18"
                    FontWeight="Bold"
                    Text="Revenue(VND)" 
                    Foreground="White"/>
                <TextBlock
                    Grid.Column="2"
                    HorizontalAlignment="Right"
                    FontSize="18"
                    FontWeight="Bold"
                    Text="Quantities" 
                    Foreground="White"/>
            </Grid>

            <!-- Revenue Data -->
            <ListView
                Grid.Row="1"
                Margin="0,40,0,0"
                ItemsSource="{Binding RevenueData}"
                Background="#3C3C3C" 
                Foreground="White"> <!-- Darker background for list -->
                <ListView.ItemTemplate>
                    <DataTemplate x:DataType="local:Detail">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="2*" />
                                <ColumnDefinition Width="1*" />
                                <ColumnDefinition Width="1*" />
                            </Grid.ColumnDefinitions>
                            <TextBlock
                                Grid.Column="0"
                                FontSize="18"
                                Text="{x:Bind Product.Name}" 
                                Foreground="White"/>
                            <TextBlock
                                Grid.Column="1"
                                HorizontalAlignment="Right"
                                FontSize="18"
                                Text="{x:Bind Cost}" 
                                Foreground="White"/>
                            <TextBlock
                                Grid.Column="2"
                                HorizontalAlignment="Right"
                                FontSize="18"
                                Text="{x:Bind Quantity}" 
                                Foreground="White"/>
                        </Grid>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </Grid>

        <!-- Total Revenue Row -->
        <Grid Grid.Row="2" Margin="20" Background="#3C3C3C" Padding="10" CornerRadius="5">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*" />
                <ColumnDefinition Width="1*" />
                <ColumnDefinition Width="1*" />
            </Grid.ColumnDefinitions>
            <TextBlock
                Grid.Column="0"
                VerticalAlignment="Center"
                FontSize="18"
                FontWeight="Bold"
                Text="Total Revenue(VND):" 
                Foreground="White"/>
            <TextBlock
                Grid.Column="1"
                HorizontalAlignment="Right"
                VerticalAlignment="Center"
                FontSize="18"
                FontWeight="Bold"
                Text="{Binding TotalRevenue}" 
                Foreground="White"/>
        </Grid>
    </Grid>
</Page>
